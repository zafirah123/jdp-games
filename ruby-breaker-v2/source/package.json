{
  "name": "ruby-breaker-v2",
  "version": "2.0.0",
  "description": "Ruby Breaker - Gem Edition Brick Breaker Game",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cp dist/index.html .. && rm -f ../assets/index-*.js && cp dist/assets/index-*.js ../assets/ && cp dist/*.png dist/*.jpeg dist/*.mp3 .."
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
